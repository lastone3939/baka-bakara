# Codex /goal Workflow

このプロジェクトでは、実装に入る前に `/goal` 型の要件定義を必ず作る。
目的は「勢いで作る」ことではなく、船を正しい進路に保つこと。

## 基本ルール

1. いきなり実装しない。
2. まず会話でウォームアップする。
3. 何を作るか、何を作らないか、どこを壊してはいけないかを書く。
4. 完了条件を数字で定義する。
5. デザインは `https://styles.refero.design/` と Lazyweb 的な参照収集を使い、参考にする。ただし丸コピーしない。
6. 実装後はスマホ幅でスクショ確認し、公開用 `dist/` とZIPを作る。

## 作る前に必ず出す要件定義

新しい機能や大きい変更では、先にこの形式で短くまとめる。

```text
/goal 要件定義

目的:
- 何を達成するか

ユーザー:
- 誰が使うか
- 誰向けではないか

期待体験:
- ユーザーが最初に何を見るか
- 何を押すか
- 成功/失敗がどう見えるか

対象範囲:
- 触る画面
- 触るファイル
- 影響する保存データ

非対象:
- 今回やらないこと

デザイン方針:
- 参考にするトーン
- 避ける見た目
- スマホで守る寸法/余白

完了条件:
- 例: 実績30個が表示される
- 例: 390px幅で横はみ出し0件
- 例: 主要操作5ルートでエラー0件

QA:
- 通常ルート
- 連打/戻る/再読込
- 破産/クリア/オールイン
- 公開用ビルド
```

## ウォームアップで聞くこと

`/goal` をタイプする前に、必要なら以下を確認する。

- ユーザーが一番気にしている画面や体験はどこか
- 「悪い状態」はどんな見た目か
- すでに試して除外したこと
- 毎回見逃しがちなバグ
- 参考にしたいアプリ、サイト、画像、過去ログ
- 自由に作ってよい場所と、忠実に再現すべき場所

## QA探索プロンプト

バグ探しだけをする時は、まず修正しない。

```text
/goal 本物のバグを狩るQAエンジニアとして振る舞う。

現実的な人間の経路でシステムを破壊せよ。
インストール、初回起動、再読込、スマホ表示、連打、ベット、オールイン、
履歴、実績、破産、クリア、公開用ビルド、Netlify投入前のファイルを確認する。

20個の独立した新しい問題を見つけたら停止。
各問題に reproduction、期待値、実際の結果、提案修正を出す。
まだ修正するな。まず狩れ。
```

## グリーンフィールド用プロンプト

```text
/goal 私たちは <X> を構築している。

参考:
- <参考プロジェクト1>
- <参考プロジェクト2>
- <repo>
- <アイデアフォルダ>
- https://styles.refero.design/
- Lazyweb で集めたUI/UX参照

アンチパターン:
- <最後の試みがやったダメなこと>
- <今回絶対に避ける見た目や仕様>

ユーザーが期待すること:
- <どう使うか>
- <誰向けか>
- <誰向けではないか>
- <創造的に作ってよい場所>
- <忠実に再現する場所>

完了条件:
- 数字で書く
- スクショで確認する画面を書く
- ビルド/公開条件を書く
```

## このゲーム固有の方針

- タイトル: 負けサビ / 1億にするまで帰れません / 負けたら泳いで帰国。
- ストーリー: 明日1億支払い。手元には韓国で負けた後の負けサビ100万円だけ。
- 体験: スマホファースト。配信で見ても一瞬で状況がわかる。
- ゲーム性: バカラの標準ドロールールを守る。P PAIRとB PAIRは別ベット。
- 挑戦制限: Supabase設定時はGoogle IDごと10回を `consume_official_attempt()` RPCで消費する。未設定時は端末ローカルで公式10回を保存し、10回後はリベンジプレイとして継続できる。
- 重要操作: ベット、オールイン、配牌、結果、履歴、実績。
- デザイン: 派手だが、操作面は読みやすく押しやすくする。
- 公開: `npm run check && npm run build` 後、`dist/` または `baccarat-100m-release.zip` をNetlifyへ入れる。
- お客さん向けUIにはSupabaseなどの裏側の管理名を出さない。
- ブラウザ操作: OSS版 Browser Harness は `uv` + Python 3.12で導入済み。詳細は `BROWSER_HARNESS.md`。

## 画像・アニメーション方針

- 画像生成が必要な時は、Codexの `imagegen` スキルを正式に使う。
- 具体的なImage2生成プロンプトは `IMAGE2_PROMPTS.md` を参照する。
- 目的は「仮素材」ではなく、100万人が遊ぶクラスを目指せるゲーム資産を作ること。
- 生成対象:
  - タイトル背景
  - ラスワンキャラ表情差分
  - 競馬/競艇/競輪のレース背景
  - クリア/破産/泳いで帰国の結果ビジュアル
  - OGP/X投稿用画像
  - ボタンや演出用の質感素材
- プロジェクトで使う画像は、生成後に必ず `assets/` 配下へ保存し、コードからそのパスを参照する。
- 透明素材が必要な時は、まずクロマキー背景で生成し、ローカルで透過処理する。
- キャラの連続アニメーションやペット風スプライトが必要な時は、`hatch-pet` 系のスプライトシート作成ワークフローも検討する。
- ただし、単純なUI形状・ボタン・グラフ・レース進行はHTML/CSS/Canvasで作り、画像に頼りすぎない。

## 実装後チェック

毎回最低限これを行う。

- `npm run check`
- `npm run build`
- スマホ幅390pxでタイトル/メイン/結果/実績を確認
- 横はみ出し0件
- ブラウザエラー0件
- Netlify用ZIP再生成
